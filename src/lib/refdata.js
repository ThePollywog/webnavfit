// Reference data (dropdowns + trait descriptors), generated from NAVFIT98A's
// Access reference DB. Imported as JSON so it's bundled at build time.
import data from "./refdata.json";
export default data;
